command:
  - uvicorn
  - app:app
  - --host
  - 0.0.0.0
  - --port
  - "8000"

env:
  - name: DATABRICKS_CATALOG
    value: ${var.catalog}
  - name: DATABRICKS_OMOP_SCHEMA
    value: ${var.omop_schema}
  - name: DATABRICKS_WAREHOUSE_ID
    value: ${var.warehouse_id}
