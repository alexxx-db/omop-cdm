resources:
  dashboards:
    healthcheck_dashboard:
      display_name: "[${bundle.target}] OMOP Healthcheck"
      file_path: ../src/dashboards/healthcheck.lvdash.json
      warehouse_id: ${var.warehouse_id}
      dataset_catalog: ${var.catalog}
      dataset_schema: ${var.omop_schema}
      permissions:
        - level: CAN_RUN
          group_name: users
